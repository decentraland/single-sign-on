export enum Action {
  SET = "set",
  GET = "get",
  REMOVE = "remove",
  CLEAR = "clear",
}

export enum Target {
  REQUEST = "iframe-local-storage-request",
  RESPONSE = "iframe-local-storage-response",
}
